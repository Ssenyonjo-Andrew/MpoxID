# Mpox Clade Classifier — requirements for offline CPU deployment
# Python 3.9–3.13 supported. CPU only — no CUDA/GPU packages required.

biopython>=1.80
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.4.0
xgboost>=2.0.0
joblib>=1.3.0
streamlit>=1.30.0
pyyaml>=6.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=6.0.0
tabulate>=0.9.0
pytest>=8.0.0

# Optional (comparison CNN only — NOT required for deployment):
# tensorflow>=2.16.0
